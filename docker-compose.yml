version: '3.8'

services:
  speedtest:
    build: .
    container_name: speedtest
    ports:
      - "${HTTP_PORT:-8080}:8080"
    volumes:
      - ./data:/data
    environment:
      - CRON_SCHEDULE=${CRON_SCHEDULE:-*/30 * * * *}
    restart: unless-stopped
