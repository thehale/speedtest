services:
  speedtest:
    build: .
    container_name: speedtest
    ports:
      - "${HTTP_PORT:-8080}:8080"
    volumes:
      - ./data:/data
    environment:
      - CRON_SCHEDULE=${CRON_SCHEDULE:-0 */5 * * *}
    restart: unless-stopped
